# gb-534 发酵动力学偏差分析

请生成一个名为 `fermentation-kinetics-deviation-analysis` 的 Go 全栈 Web 项目，面向生物制造工艺团队，用于维护发酵罐与培养配方、导入传感器时序、对齐参考动力学曲线并解释阶段性偏差。系统仅用于离线分析和决策支持，不连接或控制真实发酵罐、泵、阀门或加料系统；不得实现商城、电商、预约、订单、仓储库存、财务、客服工单、博客或大众社区。

## 1. 项目概述与复杂度下限

- 类型：B2B 前后端分离全栈 Web 应用。
- 必须实现 4 个核心实体、5 个核心页面、至少 3 个共享组件、至少 2 个 hooks、至少 4 个后端中间件和 2 组前后端一致的共享枚举。
- 实体贯穿 PostgreSQL 表、Go model/dto/repository/service/handler/router 和 Vue type/api/store/page，禁止前端绕过 API。
- 本项目用于验证跨文件协同改动能力。严禁合并职责到单一文件；字段、阶段、算法版本或权限变化必须自然触达至少 5 个文件。

## 2. 核心实体

1. `FermentationVessel`（发酵罐）
   - 字段：`id`、`vessel_code`、`name`、`working_volume_l`、`sensor_channels`、`location`、`owner_team`、`vessel_state`、`commissioned_at`、`updated_at`。
   - 关系与动作：关联多个配方和时序数据；支持建档、通道配置、停用和查看分析摘要。
   - 必须贯穿 `fermentation_vessels` 表 -> 独立 model/dto/repository/service/handler/router -> `frontend/src/types/fermentation-vessel.ts`、api、Pinia store 和罐体页面。
2. `CultureRecipe`（培养配方）
   - 字段：`id`、`vessel_id`、`recipe_code`、`version`、`organism`、`target_duration_h`、`phase_boundaries_json`、`reference_curves_json`、`tolerance_profile_json`、`recipe_state`、`created_by`。
   - 关系与动作：定义阶段边界、参考曲线和容差；支持草稿、校验、发布、废止与版本复制。
   - 必须贯穿 `culture_recipes` 表及独立后端分层、前端 type/api/store/page 文件。
3. `SensorSeries`（传感器时序）
   - 字段：`id`、`vessel_id`、`recipe_id`、`run_code`、`channel`、`sample_interval_s`、`points_json`、`started_at`、`ended_at`、`source_checksum`、`series_state`、`quality_summary`、`imported_by`。
   - 关系与动作：关联罐体与配方并被偏差分析引用；支持导入、缺失值校验、标准化、作废和重导。
   - 必须贯穿 `sensor_series` 表及独立后端分层、前端 type/api/store/page 文件。
4. `DeviationAnalysis`（偏差分析）
   - 字段：`id`、`sensor_series_id`、`recipe_version`、`algorithm_version`、`input_hash`、`phase_scores_json`、`deviation_level`、`aligned_curve_json`、`suspected_causes_json`、`analysis_state`、`explanation`、`analyzed_at`、`reviewed_by`。
   - 关系与动作：冻结时序和配方版本；支持运行、查看解释、人工确认、退回调查和作废，历史结果不可覆盖。
   - 必须贯穿 `deviation_analyses` 表及独立后端分层、前端 type/api/store/page 文件。

## 3. 差异化算法与状态流

- 先按时间戳排序、去重并记录缺失率；使用稳健缩放处理 pH、温度、溶氧、搅拌转速等不同量纲，禁止静默填补大段缺失数据。
- 以配方阶段边界为约束，使用动态时间规整对齐实测与参考曲线；计算阶段持续时间、斜率、峰值时间、曲线距离和多通道加权偏差，并输出逐阶段证据与疑似原因规则命中。
- `SensorSeries`：`imported -> validated -> normalized -> ready`，失败到 `rejected`，`ready -> superseded`；非法迁移返回 409。
- `DeviationAnalysis`：`queued -> analyzing -> completed/failed -> reviewed -> confirmed/investigating/voided`；同一输入哈希和算法版本幂等。
- 算法必须确定、可重放并有固定夹具测试；只辅助工艺人员分析，不输出真实设备设定值或控制指令。

## 4. 核心页面与实体消费

1. `/vessels` 罐体总览：消费 `FermentationVessel + SensorSeries`，显示通道和最近数据质量。
2. `/recipes` 配方版本：消费 `CultureRecipe + FermentationVessel`，编辑阶段边界、参考曲线和容差版本。
3. `/series` 时序工作台：消费 `SensorSeries + FermentationVessel + CultureRecipe`，导入数据并展示质量问题。
4. `/analyses` 偏差分析：消费 `DeviationAnalysis + SensorSeries + CultureRecipe`，展示对齐曲线、阶段评分和解释。
5. `/audit` 审计中心：消费四实体变更投影和审计日志，按实体、request ID、操作者和时间过滤。

`PhaseBadge` 被配方、时序和分析页共用；`KineticsChart` 被时序和分析页共用；`AnalysisExplanationDrawer` 被分析、配方和审计页共用。

## 5. 横切关注点

- JWT + RBAC：角色至少为 `admin`、`process_scientist`、`data_analyst`、`reviewer`、`auditor`；联动角色表、JWT、Go auth/rbac、路由、Vue guard、按钮显隐和类型。分析发起者不能确认自己的结果。
- 审计与追踪：写操作记录前后快照、request ID 和操作者；算法记录输入 checksum、配方/算法版本、参数、耗时和摘要。
- 全局错误：统一错误响应；时间序列格式/质量不足返回 422，版本和状态冲突返回 409，前端统一处理。
- 限流与幂等：登录、导入和分析接口独立限流；分析用 `Idempotency-Key`，并发运行使用事务保护。

## 6. 共享枚举、组件、hooks 与中间件

- `FermentationPhase = lag | growth | production | harvest`：后端 `backend/internal/constants/fermentation_phase.go`，前端 `frontend/src/types/enums/fermentation-phase.ts`。
- `DeviationLevel = normal | watch | major | critical`：后端 `backend/internal/constants/deviation_level.go`，前端 `frontend/src/types/enums/deviation-level.ts`。
- README 列出枚举在 model、dto、算法、service 状态机、store、组件、页面和测试中的所有位置。
- 共享组件：`PhaseBadge`、`KineticsChart`、`AnalysisExplanationDrawer`；hooks/composables：`useAuth`、`useAnalysisRun`。
- 中间件至少拆为 `request_id.go`、`recovery.go`、`auth.go`、`rbac.go`、`audit.go`、`error_handler.go`。

## 7. 技术、规模与文件结构

- 英文短名：`fermentation-kinetics-deviation-analysis`。
- 前端：Vue 3 + TypeScript + Vite + Element Plus + Pinia + ECharts；后端：Go 1.22 + Gin + GORM；正式 PostgreSQL 16，runtime smoke 支持 SQLite；JWT。
- API 统一使用 `/api/v1`，另提供真实 `/healthz`；使用 validator/v10、统一响应和错误码、`%w` 错误链、request ID、结构化日志、构造器注入并实现优雅停机。多表写入用事务，状态迁移用条件更新，唯一性/幂等不能只靠前端。禁止 TODO、placeholder、空函数、硬编码成功响应和 handler 直连数据库。
- Go 功能代码 2500-4200 行、24-42 个功能 `.go` 文件且少于 5000 行；不计 `_test.go`、生成代码、vendor。时序校验、DTW、评分、状态机和权限须有测试。
- 只保留普通项目单测；不得创建名称含 record/regression/grading/scoring 的评分测试，不得加入答案、`BUG_REPRO.md` 或标注流水线内部文件。
- 根 `go.work` 纳入 `./backend`，支持根目录 `go work sync`、`go build ./backend/...`、`go test ./backend/...`。

```text
.
├── go.work  runtime_smoke.json
├── docker-compose.yml  .env  .env.example  .gitignore  README.md
├── output/
│   └── execution.md
├── backend/
│   ├── go.mod  Dockerfile  cmd/server/main.go
│   └── internal/
│       ├── config/ constants/ dto/ model/
│       ├── repository/ service/ handler/ router/
│       ├── middleware/ algorithm/ timeseries/ util/
├── frontend/
│   ├── package.json  Dockerfile  nginx.conf
│   └── src/
│       ├── api/ stores/ types/ components/common/
│       ├── hooks/ pages/ router/ utils/
└── database/init.sql
```

四实体必须在全部后端层与前端 api/store/type 中按实体分文件。严禁合并职责到单一文件，禁止单个通用 CRUD 文件承载所有业务。

## 8. 运行 Manifest、部署与验证交付

- 固定端口：前端 `18534`、后端 `19534`、PostgreSQL 宿主机 `57534`；容器内 `80/8080/5432`。
- 根 `runtime_smoke.json` 的唯一用途是提供可解析的启动 manifest，内容必须严格等价于：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20534",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "JWT_SECRET": "runtime-smoke-only-change-me"
  },
  "ready_url": "http://127.0.0.1:20534/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

- 后端必须支持 SQLite smoke 配置并自动建表/种子数据；正式 Compose 仍使用 PostgreSQL。manifest 禁止出现 `passed`、截图或执行证据字段。
- Compose 顶层 `name: fermentation-kinetics-deviation-analysis` 且无 `version:`；编排 db/backend/frontend，使用命名卷和英文短名容器名前缀，数据库、后端和前端均有 healthcheck，并按健康条件依赖。
- `.env`、`.env.example` 均含 `COMPOSE_PROJECT_NAME=fermentation-kinetics-deviation-analysis`、`FRONTEND_PORT=18534`、`BACKEND_PORT=19534`、`DB_PORT=57534`、`DB_DRIVER=postgres`、DB 变量与 `JWT_SECRET`；`.env` 不提交。前端仅调用 `/api`，Nginx 原样代理 `/api/v1` 到 `backend:8080`，另提供 `/api/healthz` 并支持 SPA。
- README 首屏为 `docker compose up -d`，说明项目介绍、主要功能、角色账号、架构与目录、API 清单、技术栈表、环境变量与端口、本地开发、Docker 部署、枚举位置、算法边界、runtime smoke、构建测试、常见问题、停止方式和 License；README 不得承载执行证据。
- 真实运行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`、`npm --prefix frontend ci`、`npm --prefix frontend run build`、`python3 /Users/gaobo/.codex/skills/go-annotation-pipeline/scripts/project_scale.py .`、`python3 /Users/gaobo/.codex/skills/go-annotation-pipeline/scripts/runtime_smoke.py .`、`docker compose config --quiet`、`docker compose up -d --build`、`docker compose ps`；前后端均须有 healthcheck，所有长驻服务必须 healthy。
- 至少 8 个真实 API 冒烟：health、登录、四实体查询、罐体/配方创建、时序导入、合法/非法迁移、分析运行与读取、403 权限，覆盖成功和失败分支。
- API 冒烟还必须包含详情读取、未认证 401 与越权 403；四个核心实体各至少经过一条真实后端链路，五个页面都消费真实 `/api/v1`，不得使用静态 mock。并在 `backend/` 内复跑 `go build ./...`、`go vet ./...`、`go test ./...`。
- 仅用 Codex 内置 Browser 验证登录、罐体、配方、时序、分析五页，禁止外部 Chrome；实际完成创建或编辑、列表刷新和一次状态变化，并检查 console/network、文字溢出和遮挡。
- 所有构建、测试、runtime smoke、Compose、至少 8 条 API、Browser、console/network 和关闭证据只能写入 `output/execution.md`；截图必须直接放 `output/`，至少桌面和移动端各一张并由 execution.md 引用，禁止在其他文件复制证据或伪造结果。
- `output/execution.md` 必须包含项目编号、验证时间、实际端口、规模实测、根与 backend 构建/vet/test、前端构建、服务 healthy、API 表、Browser 操作步骤、截图相对路径、逐条需求覆盖、修复摘要、停服无残留和实现提交 hash。
- 验证后执行 `docker compose down -v --remove-orphans`，确认容器、网络和命名卷均已清理，并停止 runtime smoke 进程。
- 项目必须独立 `git init`，设置本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。真实实现与验证完成后提交 `git add -A && git commit -m "feat: implement and verify fermentation-kinetics-deviation-analysis"`，把实现提交 hash 写入 `output/execution.md`，再执行 `git add output/execution.md && git commit -m "docs: record verified execution"`，形成两次提交。`.gitignore` 排除 `.env`、依赖、构建产物、日志和本地 SQLite 文件，但保留 execution.md 与截图；最终 `git status --short` 为空，不得共用 Git 仓库。
